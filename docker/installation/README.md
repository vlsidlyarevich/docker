###Bash for installing Docker

* installs docker

#####Running
type following commands in terminal:
* `sudo sh docker.sh`
* `sudo groupadd docker`
* `sudo usermod -aG docker ubuntu` where ubuntu is user name
* `sudo reboot`
